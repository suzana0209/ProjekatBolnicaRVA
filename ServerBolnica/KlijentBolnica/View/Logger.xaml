<Window x:Class="KlijentBolnica.View.Logger"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KlijentBolnica.View"
        mc:Ignorable="d"
        Title="LogView" Height="300" Width="546.117">
    <Grid>
        <DataGrid x:Name="dgridLogs" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="250" Width="518" ItemsSource="{Binding Logs}"
                  AutoGenerateColumns="false" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Timestamp}" Header="Timestamp" />
                <DataGridTextColumn Binding="{Binding Path=Level}" Header="Level" />
                <DataGridTextColumn Binding="{Binding Path=Message}" Header="Message" Width="3*"  />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
